"""
toolbox/ffmpeg — video assembly, conversion, and media processing via FFmpeg.
Entry point: ffmpeg_tool.run(description, params)
Requires FFmpeg installed on the system — see brain/config.py for FFMPEG_PATH.
"""
